# Service Mobile App

Service-focused Flutter app scaffold for complaint lifecycle operations:
- queue by stage,
- complaint detail,
- visit/test capture,
- telephonic closure,
- retest handling,
- role-driven actions.

This app reuses the same auth/network/router patterns as `sales_mobile_app` and targets new backend `service*` procedures.
